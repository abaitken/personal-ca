function Compare(left, right) {
    if (left < right)
        return -1;

    if (left > right)
        return 1;

    return 0;
}

(() => {
    'use strict'

    feather.replace({ 'aria-hidden': 'true' });
    
})();